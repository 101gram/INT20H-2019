import Express from 'express';

export const router = Express.Router()
.get('/api/photo-emotions', async (_req, _res) => {

    // const obj = new FacePlusPlus(Config.FacePP.ApiKey, Config.FacePP.ApiSecret);
    // ну да)) heh
    // const result = await obj.getFacesEmotions(""); // throws
});